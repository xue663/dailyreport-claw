# 🤖 Daily Report Claw - OpenClaw 实时监控控制台

专为 **OpenClaw** 设计的实时监控Dashboard，自动追踪任务执行、系统资源使用、用户互动记录，支持智能反思和定时任务管理。

![Dashboard Preview](https://img.shields.io/badge/OpenClaw-Ready-blue)
![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)
![Status](https://img.shields.io/badge/Status-Production%20Ready-success)

## ✨ 核心特性

- 🎯 **OpenClaw深度集成** - 自动读取会话历史、任务状态、系统信息
- ⚡ **实时任务追踪** - 自动记录用户任务和系统任务状态
- 🕐 **定时任务支持** - 支持scheduled状态，区分用户任务和系统任务
- 🤔 **智能反思系统** - AI自动生成每日反思和明日计划
- 📊 **零依赖运行** - 仅使用Python标准库，开箱即用
- 🎨 **科技风UI** - 深色主题 + 霓虹效果，实时自动刷新
- 🔄 **RESTful API** - 提供任务创建、更新、查询接口

## 🎬 快速预览

```
┌─────────────────────────────────────────────────────────┐
│  🤖 阿呆控制台                     [今天▼] [🔄 刷新]      │
├─────────────────────────────────────────────────────────┤
│  ⚡ 系统状态              📊 任务统计                    │
│  • OpenClaw: 2026.2.1 ✅  • 完成: 6                     │
│  • Gateway: 运行中 ✅     • 失败: 0                     │
│  • Model: GLM-4.7 🟢     • 进行中: 0                   │
│  • CPU: 12% ▓▓▓░░░░░     • 计划: 3                     │
│  • 内存: 45% ▓▓▓▓░░░░  • 互动: 28                    │
├─────────────────────────────────────────────────────────┤
│  🎯 任务执行流                                         │
│  1. ✅ [00:37] 实现任务类型区分 (用户任务)             │
│  2. 🕐 [09:00] 审查今日任务质量 (系统任务)              │
│  3. 🕐 [09:00] 检查系统运行状态 (系统任务)              │
├─────────────────────────────────────────────────────────┤
│  💬 今日互动              🤔 反思 & 产出                │
│  • 更新GitHub项目         • 今日收获                    │
│    已同步上传...           • 识别3个优化点              │
│  • 任务类型区分          • 明日计划                    │
│    系统任务vs...           • 3个待办事项                │
└─────────────────────────────────────────────────────────┘
```

## 🚀 快速开始

### 环境要求

- Python 3.8+
- OpenClaw 已安装并运行
- 无需任何第三方依赖

### 一键安装

```bash
# 克隆仓库
git clone https://github.com/xue663/dailyreport-claw.git
cd dailyreport-claw

# 运行安装脚本
./scripts/install.sh

# 启动服务
./scripts/start.sh
```

### 访问控制台

打开浏览器访问: http://localhost:8080

局域网访问: http://YOUR_IP:8080

## 📁 项目结构

```
dailyreport-claw/
├── server.py                    # Web服务器（零依赖）
├── config.json                  # 配置文件
├── src/
│   ├── data_collector.py        # OpenClaw数据收集
│   └── system_monitor.py        # 系统资源监控
├── data/                        # 数据存储目录
│   ├── user_tasks.json          # 用户任务记录
│   ├── interactions.json        # 互动记录
│   ├── reflection.json          # 反思内容
│   └── scheduled_tasks.json     # 定时任务配置
├── web/
│   ├── index.html               # Dashboard页面
│   └── static/
│       ├── style.css            # 深色主题样式
│       └── app.js               # 前端逻辑（实时刷新）
├── scripts/
│   ├── generate_reflection.py   # 反思生成脚本
│   ├── task_listener.py         # 任务监听器
│   ├── install.sh               # 安装脚本
│   ├── start.sh                 # 启动脚本
│   └── setup-autostart.sh       # 自启动配置
├── logs/                        # 日志目录
├── README.md
└── LICENSE
```

## ⚙️ 配置说明

### config.json

```json
{
  "server": {
    "host": "0.0.0.0",
    "port": 8080
  },
  "openclaw": {
    "agents_dir": "~/.openclaw/agents/main/agents",
    "sessions_dir": "~/.openclaw/agents/main/sessions",
    "workspace_dir": "~/.openclaw/workspace"
  },
  "data": {
    "refresh_interval": 30,
    "max_tasks_display": 50,
    "max_interactions_display": 20
  }
}
```

### OpenClaw 适配

控制台会自动读取以下OpenClaw数据：

- **会话历史** - `~/.openclaw/agents/main/sessions/*.jsonl`
- **任务状态** - 从会话中提取用户任务
- **系统信息** - OpenClaw版本、运行时间、模型信息
- **TOKENS统计** - 累计使用的token数量
- **用户互动** - 所有对话记录

## 🔌 API 接口

### 获取Dashboard数据

```bash
GET /api/data/{time_filter}
```

**参数:**
- `time_filter`: `today` | `week` | `month` | `all`

**返回示例:**
```json
{
  "system": {
    "openclaw_version": "2026.2.1",
    "gateway_status": "running",
    "model": "GLM-4.7",
    "cpu_percent": 12,
    "memory_percent": 45,
    "uptime": "2h 15m",
    "tokens_total": 1250000
  },
  "stats": {
    "completed": 6,
    "failed": 0,
    "running": 0,
    "interactions": 28,
    "files_created": 5
  },
  "tasks": [
    {
      "id": "task_1234567890",
      "description": "实现任务类型区分",
      "status": "completed",
      "task_type": "user_task",
      "created_at": "2026-02-06T00:37:00"
    }
  ],
  "interactions": [ ... ],
  "reflection": { ... }
}
```

### 创建任务

```bash
POST /api/task/create
Content-Type: application/json

{
  "description": "任务描述",
  "user_message": "原始用户消息",
  "status": "running",  // running | scheduled
  "scheduled_time": "2026-02-06T09:00:00"  // 可选，仅scheduled任务
}
```

### 更新任务状态

```bash
POST /api/task/update
Content-Type: application/json

{
  "task_id": "task_1234567890",
  "status": "completed",  // running | completed | failed
  "result": "执行结果摘要"
}
```

### 健康检查

```bash
GET /health
```

## 🎯 任务类型说明

控制台支持两种任务类型：

### 用户任务 (user_task)
- **来源**: 用户主动交办的实时任务
- **状态**: `running` → `completed` / `failed`
- **示例**: "帮我查天气"、"优化这个代码"

### 系统任务 (system_task)
- **来源**: 反思系统自动创建的定时提醒
- **状态**: `scheduled` → `running` → `completed`
- **图标**: 🕐 计划任务
- **示例**: 明日计划任务、定时提醒

## 🤔 智能反思系统

### 自动反思生成

```bash
# 手动触发反思生成
curl -X POST http://localhost:8080/api/reflection/generate

# 或运行脚本
python3 scripts/generate_reflection.py
```

### 反思内容

- **今日收获** - 完成的任务、解决的问题
- **改进建议** - 识别可优化的环节
- **明日计划** - 自动创建定时任务（明天上午9点执行）

### 定时任务

反思系统会自动创建定时任务：

```json
{
  "plan": "审查今日完成的任务质量",
  "task_id": "task_1234567890",
  "scheduled_time": "2026-02-06T09:00:00"
}
```

## 🛠️ 高级功能

### 任务监听器

自动监听OpenClaw会话并提取任务：

```bash
# 启动监听器
./scripts/start_listener.sh

# 查看状态
ps aux | grep task_listener

# 停止监听器
./scripts/stop_listener.sh
```

### 自启动配置

```bash
# 设置开机自启动
./scripts/setup-autostart.sh

# 查看服务状态
systemctl --user status dailyreport-claw
```

### 系统服务管理

```bash
# 启动服务
systemctl --user start dailyreport-claw

# 停止服务
systemctl --user stop dailyreport-claw

# 重启服务
systemctl --user restart dailyreport-claw
```

## 🎨 自定义主题

编辑 `web/static/style.css`:

```css
:root {
  --neon-blue: #00f0ff;      /* 主色调 */
  --neon-purple: #b026ff;    /* 强调色 */
  --neon-green: #39ff14;     /* 成功色 */
  --neon-red: #ff073a;       /* 警告色 */
  --bg-color: #0a0e27;       /* 背景色 */
  --card-bg: #151b3d;        /* 卡片背景 */
}
```

## 📊 数据展示

### 任务状态图标

- ✅ `completed` - 已完成
- ❌ `failed` - 失败
- 🔄 `running` - 执行中
- 🕐 `scheduled` - 计划任务

### 系统状态指标

- **OpenClaw版本** - 从 `openclaw --version` 获取
- **Gateway状态** - 运行中/已停止
- **模型信息** - 当前使用的模型
- **CPU/内存** - 实时资源使用率
- **运行时间** - OpenClaw启动时长
- **TOKENS统计** - 累计token消耗

## 🔧 故障排查

### 端口被占用

```bash
# 查找占用进程
lsof -i :8080

# 杀死进程
kill -9 <PID>

# 或修改config.json中的端口
```

### OpenClaw数据读取失败

```bash
# 检查路径配置
cat config.json | grep openclaw

# 确保路径存在
ls -la ~/.openclaw/agents/main/sessions/

# 手动测试数据收集
python3 -c "from src.data_collector import DataCollector; import json; dc = DataCollector('config.json'); print(json.dumps(dc.get_tasks('today'), indent=2))"
```

### 任务监听器无响应

```bash
# 查看监听器日志
tail -f logs/task_listener.log

# 重启监听器
./scripts/stop_listener.sh
./scripts/start_listener.sh
```

## 🚧 开发路线图

- [x] 任务类型区分（用户任务 vs 系统任务）
- [x] Scheduled状态支持
- [x] 智能反思生成
- [x] 明日计划自动创建
- [x] OpenClaw深度集成
- [ ] 图表可视化（任务完成趋势）
- [ ] 多主题支持（浅色/深色）
- [ ] 数据导出（CSV/PDF）
- [ ] 任务搜索和过滤
- [ ] 移动端适配
- [ ] 多用户支持
- [ ] 认证和权限管理

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

### 开发环境

```bash
# 克隆仓库
git clone https://github.com/xue663/dailyreport-claw.git
cd dailyreport-claw

# 创建虚拟环境（可选）
python3 -m venv venv
source venv/bin/activate

# 运行服务器
python3 server.py
```

### 代码风格

- 遵循 PEP 8 规范
- 添加类型注解
- 编写单元测试
- 更新文档

## 📄 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

- **OpenClaw** - 强大的AI助手框架
- **Python标准库** - 零依赖设计的基石
- **Cyberpunk设计** - UI风格灵感来源

## 📞 联系方式

- GitHub Issues: https://github.com/xue663/dailyreport-claw/issues
- OpenClaw社区: https://discord.com/invite/clawd

---

**Made with ❤️ for OpenClaw Community**

⭐ Star this repo if you find it helpful!
