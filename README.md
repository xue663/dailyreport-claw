# 🤖 AI Assistant Dashboard - 实时监控控制台

一个现代化的AI助手监控Dashboard，实时追踪任务执行状态、系统资源使用情况和用户互动记录。

![Dashboard Preview](https://img.shields.io/badge/Status-Production%20Ready-success)
![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![License](https://img.shields.io/badge/License-MIT-green.svg)

## ✨ 特性

- 🎯 **单屏展示** - 所有信息一屏掌控，无需滚动
- ⚡ **实时刷新** - 自动更新数据，可配置刷新间隔
- 🎨 **科技风设计** - 深色主题 + 霓虹效果
- 📊 **任务追踪** - 自动记录任务执行状态
- 💬 **互动记录** - 展示用户与AI的对话历史
- 🤔 **智能反思** - AI自我改进建议
- 🔧 **系统监控** - 实时系统资源监控
- 🚀 **零依赖** - 使用Python标准库，无需安装第三方包

## 🎬 截图

```
┌─────────────────────────────────────────────────────────┐
│  🤖 AI控制台                        [今天▼] [🔄 刷新]      │
├─────────────────────────────────────────────────────────┤
│  ⚡ 系统状态              📊 任务统计                    │
│  • OpenClaw: v1.0.0 ✅   • 完成: 42                     │
│  • Gateway: 运行中 ✅    • 失败: 8                      │
│  • Model: GLM-4.7       • 进行中: 0                    │
│  • CPU: 12% ▓▓▓░░░░░   • 互动: 156                    │
│  • 内存: 45% ▓▓▓▓░░░░  • 产出: 12                     │
├─────────────────────────────────────────────────────────┤
│  🎯 任务执行流                                         │
│  1. ✅ [刚刚] 查询合肥今天的天气                      │
│  2. 🔄 [2分钟前] 分析系统性能数据                      │
│  3. ✅ [5分钟前] 了解旺财狗项目                        │
├─────────────────────────────────────────────────────────┤
│  💬 今日互动              🤔 反思 & 产出                │
│  • 帮我查天气            • 改进建议                     │
│    我已经查询...          • 今日收获                     │
│  • 分析这个项目          • 明日计划                     │
└─────────────────────────────────────────────────────────┘
```

## 🚀 快速开始

### 环境要求

- Python 3.8+
- 无需安装任何第三方依赖

### 安装

```bash
# 克隆仓库
git clone https://github.com/your-username/ai-dashboard.git
cd ai-dashboard

# 复制配置文件
cp config.example.json config.json

# 编辑配置（可选）
vim config.json
```

### 启动

```bash
# 方式1: 直接启动
python3 server.py

# 方式2: 后台运行
nohup python3 server.py > server.log 2>&1 &

# 方式3: 使用启动脚本
./start.sh
```

### 访问

打开浏览器访问: http://localhost:8080

## 📁 项目结构

```
ai-dashboard/
├── server.py                 # Web服务器主程序
├── config.json               # 配置文件
├── config.example.json       # 配置文件示例
├── src/
│   ├── data_collector.py     # 数据收集模块
│   ├── system_monitor.py     # 系统监控模块
│   └── __init__.py
├── data/                     # 数据目录（自动创建）
│   ├── user_tasks.json       # 用户任务
│   ├── interactions.json     # 互动记录
│   └── system_status.json    # 系统状态
├── web/
│   ├── index.html            # Dashboard页面
│   └── static/
│       ├── style.css         # 样式文件
│       └── app.js            # 前端逻辑
├── scripts/
│   ├── start.sh              # 启动脚本
│   ├── task_listener_simple.py  # 任务监听器
│   └── stop_listener.sh      # 停止监听器
├── logs/                     # 日志目录（自动创建）
├── .gitignore
├── README.md
└── LICENSE
```

## ⚙️ 配置

编辑 `config.json`:

```json
{
  "server": {
    "host": "0.0.0.0",
    "port": 8080,
    "debug": false
  },
  "data": {
    "refresh_interval": 30,
    "max_tasks_display": 50,
    "max_interactions_display": 20
  },
  "system": {
    "openclaw_path": "/path/to/openclaw",
    "workspace_path": "/path/to/workspace"
  }
}
```

### 配置说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `server.host` | 监听地址 | `0.0.0.0` |
| `server.port` | 监听端口 | `8080` |
| `data.refresh_interval` | 刷新间隔（秒） | `30` |
| `data.max_tasks_display` | 最大显示任务数 | `50` |
| `system.openclaw_path` | OpenClaw路径 | 自动检测 |

## 🔌 API 端点

### 获取数据
```
GET /api/data/{time_filter}
```

**参数:**
- `time_filter`: `today` | `week` | `month` | `all`

**返回:**
```json
{
  "system": { ... },
  "stats": { ... },
  "tasks": [ ... ],
  "interactions": [ ... ],
  "reflection": { ... }
}
```

### 健康检查
```
GET /health
```

### 系统状态
```
GET /api/system
```

## 🎨 自定义

### 修改主题颜色

编辑 `web/static/style.css`:

```css
:root {
  --neon-blue: #00f0ff;
  --neon-purple: #b026ff;
  --neon-green: #39ff14;
  --neon-red: #ff073a;
  --bg-color: #0a0e27;
}
```

### 修改刷新间隔

编辑 `web/static/app.js`:

```javascript
// 30秒改为60秒
autoRefresh = setInterval(refreshData, 60000);
```

## 🔧 故障排查

### 端口被占用

```bash
# 查找占用进程
lsof -i :8080

# 杀死进程
kill -9 <PID>

# 或修改配置文件中的端口
```

### 权限问题

```bash
# 确保数据目录可写
chmod +x data/
chmod +x logs/
```

### 查看日志

```bash
# 实时查看服务器日志
tail -f server.log

# 查看监听器日志
tail -f logs/task_listener.log
```

## 📊 任务监听器

任务监听器会自动从会话历史中提取用户任务：

```bash
# 启动监听器
./scripts/start_listener.sh

# 停止监听器
./scripts/stop_listener.sh

# 查看状态
ps aux | grep task_listener
```

**监听器功能:**
- 自动检测新的用户消息
- 智能提取任务描述
- 实时更新任务状态

## 🛠️ 开发

### 添加新的数据源

编辑 `src/data_collector.py`:

```python
def get_custom_data(self, time_filter='today'):
    """获取自定义数据"""
    # 你的逻辑
    return {"data": "..."}
```

### 添加新的API端点

编辑 `server.py`:

```python
def handle_custom_request(self):
    """处理自定义请求"""
    data = self.data_collector.get_custom_data()
    self.send_json_response(data)
```

## 📝 待办事项

- [ ] 添加图表可视化
- [ ] 支持多主题切换
- [ ] 添加导出功能（PDF/CSV）
- [ ] 实现任务搜索
- [ ] 添加系统通知
- [ ] 支持多用户
- [ ] 添加认证功能

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 License

MIT License - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

- 深色主题设计灵感来自 Cyberpunk 风格
- 零依赖设计，感谢 Python 标准库

---

**Made with ❤️ by AI Assistant**

Star ⭐ this repo if you find it helpful!
